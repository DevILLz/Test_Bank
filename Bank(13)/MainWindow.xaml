<Window x:Class="Bank_13_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bank_13_"
        mc:Ignorable="d"
        Title="Банк" MinHeight="450" MinWidth="800" Width="850"  Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="650" Width="*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Name="ClientsList" SelectionMode="Extended" Grid.Column="0" ItemsSource="{Binding}" AutoGenerateColumns="False" Grid.Row="1" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" MaxWidth="35" IsReadOnly="True"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FullName}" Width="180"/>
                <DataGridTextColumn Header="Адрес"  Binding="{Binding Address}" Width="150"/>
                <DataGridTextColumn Header="Телефон"  Binding="{Binding PNuber}" MinWidth="100" MaxWidth="100" />
                <DataGridTextColumn Header="Счет"  Binding="{Binding Money}" MinWidth="70" MaxWidth="70" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        <Menu Grid.Row="0" Grid.ColumnSpan="3" >
            <MenuItem Header="File">
                <!--Импорт данных в таблицу из выбранного места-->
                <MenuItem Header="Импорт данных в таблицу" Click="Import_button"/>
                <!--Экспорт данных из таблицы в выбранное место-->
                <MenuItem Header="Экспорт данных из таблицы" Click="Export_button"/>
                <MenuItem Header="Создать новую БД" Click="NewDB_button"/>
            </MenuItem>
            <MenuItem Header="Добавить клиента" Click="Add_button"/>
            <MenuItem Header="Меню иммитации работы системы" Click="Immitation"/>
        </Menu>
        <ListBox x:Name="OperationList" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</Window>
